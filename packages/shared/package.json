{
  "name": "@monorepo/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and utilities for the Entra Auth PoC",
  "main": "./index.ts",
  "module": "dist/index.mjs",
  "types": "./index.ts",
  "scripts": {
    "build": "echo 'Add build command here'",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "lint": "eslint src/ --fix",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.14.1",
    "tsup": "^8.4.0",
    "typescript": "^5.8.2"
  }
}
